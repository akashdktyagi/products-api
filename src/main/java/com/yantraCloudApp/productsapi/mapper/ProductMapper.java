package com.yantraCloudApp.productsapi.mapper;

import com.yantraCloudApp.productsapi.model.Product;
//import org.apache.ibatis.annotations.*;

import java.util.List;

//@Mapper
public interface ProductMapper {

//    @Insert("insert into products(userID,name,description,expectedPrice,quantity) " +
//            "values(#{userID},#{name},#{description},#{expectedPrice},#{quantity})")
//    @Options(useGeneratedKeys = true, keyProperty = "id", keyColumn = "id")
//    @SelectKey(statement = "SELECT LAST_INSERT_ID()", keyProperty = "id",
//            before = false, resultType = Integer.class)
//    int insertProduct(Product product);
//
//    @Select("select * from products")
//    List<Product> getProducts();
//
//    @Select("select * from products where name = #{productName}")
//    List<Product> getProductWithProductNameAs(@Param("productName") String productName);
}
